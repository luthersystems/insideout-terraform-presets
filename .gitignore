# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Temporary files
.tmp/

# insideout-import binary (at root only, not cmd/insideout-import/)
/insideout-import
imported/

# Environment files (credentials)
.env

# Claude Code local settings (keep shared .claude/settings.json)
.claude/settings.local.json
