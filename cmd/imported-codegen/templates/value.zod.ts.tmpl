// Code generated by cmd/imported-codegen; DO NOT EDIT.
// @generated
import { z } from "zod";

// expressionAware wraps a leaf Zod schema so a value can be expressed
// as a Terraform literal, a Terraform expression string, or an explicit
// null. Mirrors the Go side's *Value[T] four-way distinction
// (absent / explicit null / literal / expression).
export const expressionAware = <T extends z.ZodTypeAny>(t: T) =>
  z.union([
    z.object({ literal: t }),
    z.object({ expr: z.string() }),
    z.object({ null: z.literal(true) }),
  ]);

// FieldMeta is the per-attribute provider metadata shape. Matches the
// JSON wire shape of generated.FieldSchema (lowercased keys,
// omitempty-style absent fields).
export type FieldMeta = {
  required?: boolean;
  optional?: boolean;
  computed?: boolean;
  sensitive?: boolean;
  replacement?: "unknown" | "never" | "may_replace" | "always_replace";
};
